# Supabase Configuration
# Get these values from your Supabase project settings

# Public keys (safe for browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-only keys (never expose to client)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Site URL (required for email verification links in production)
NEXT_PUBLIC_SITE_URL=https://sitesistersconstruction.com

# Development Settings
# Set to 'true' to auto-confirm user emails during local development
# This skips email verification so you don't need to check emails while testing
NEXT_PUBLIC_DEV_AUTO_CONFIRM=true

# Google Maps (optional - for address autocomplete)
# NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-key
